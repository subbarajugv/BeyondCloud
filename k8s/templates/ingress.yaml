{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "beyondcloud.fullname" . }}
  labels:
    {{- include "beyondcloud.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                {{- if eq .service "frontend" }}
                name: {{ include "beyondcloud.fullname" $ }}-frontend
                port:
                  number: {{ $.Values.frontend.service.port }}
                {{- else if eq .service "nodejsBackend" }}
                name: {{ include "beyondcloud.fullname" $ }}-nodejs
                port:
                  number: {{ $.Values.nodejsBackend.service.port }}
                {{- else if eq .service "pythonBackend" }}
                name: {{ include "beyondcloud.fullname" $ }}-python
                port:
                  number: {{ $.Values.pythonBackend.service.port }}
                {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
